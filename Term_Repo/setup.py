from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='predict if customers will subscribe to a term deposit using categorical and numerical survey data.',
    author='Noah_Cox',
    license='MIT',
)
